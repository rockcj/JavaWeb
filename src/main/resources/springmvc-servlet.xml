<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 启用注解扫描，扫描Controller包 -->
    <context:component-scan base-package="cn.edu.lingnan.controller"/>

    <!-- 启用Spring MVC注解驱动 -->
    <mvc:annotation-driven/>

    <!-- 配置处理器映射器：RequestMappingHandlerMapping（注解驱动自动配置） -->
    <!-- 配置处理器适配器：RequestMappingHandlerAdapter（注解驱动自动配置） -->

    <!-- 配置视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!-- 视图前缀：JSP文件在WEB-INF/views目录下 -->
        <property name="prefix" value="/WEB-INF/views/"/>
        <!-- 视图后缀 -->
        <property name="suffix" value=".jsp"/>
        <!-- 允许直接访问JSP文件 -->
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <!-- 设置order，让这个视图解析器优先级较低，允许forward到web根目录的JSP -->
        <property name="order" value="1"/>
    </bean>
    
    <!-- 配置第二个视图解析器，用于处理web根目录下的JSP（如register.jsp） -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"/>
        <property name="suffix" value=".jsp"/>
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="order" value="2"/>
    </bean>

    <!-- 静态资源处理 -->
    <mvc:default-servlet-handler/>

</beans>

