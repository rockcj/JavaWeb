<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 配置设置 -->
    <settings>
        <!-- 开启驼峰命名自动映射，从经典数据库列名A_COLUMN到经典Java属性名aColumn的类似映射 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <!-- 配置环境 -->
    <environments default="development">
        <environment id="development">
            <!-- 事务管理器类型：JDBC -->
            <transactionManager type="JDBC"/>
            <!-- 数据源类型：POOLED -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/school?useSSL=false&amp;serverTimezone=UTC&amp;characterEncoding=UTF-8"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 注意：Mapper XML文件通过Spring的applicationContext.xml中的mapperLocations配置扫描 -->
    <!-- 但是使用注解的Mapper接口需要在这里注册 -->
    <mappers>
        <!-- 注册使用注解的Mapper接口 -->
        <package name="cn.edu.lingnan.mapper"/>
    </mappers>

</configuration>